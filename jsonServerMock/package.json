{
  "name": "jsonservermock",
  "version": "1.0.0",
  "description": "1. `npm install -g json-server` 全局安装json-server",
  "main": "db.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mockjs": "^1.1.0"
  }
}
